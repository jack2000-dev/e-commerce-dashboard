version: 2

models:
  - name: fct_order_fulfillment
    columns:
      - name: order_id
        tests:
          - not_null
          - unique
  - name: fct_seller_order_fulfillment
    columns:
      - name: order_id
        tests:
          - not_null
      - name: seller_id
        tests:
          - not_null